import LogoutButton from "@/components/LogoutButton";

export default function ListPage() {
  return (
    <div>
      <h2>Lista de Usuários</h2>
      <p>Aqui está a lista de usuários cadastrados.</p>
      <LogoutButton />
    </div>
  );
}
